# Contributing

Keep shared Toolbox behavior behind module interfaces and OS-specific behavior in named host
projections. Preserve installed-user identities and data paths unless a separate migration decision
and rollback test approve a change. Do not create an empty Linux implementation for symmetry.
